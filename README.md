# 🔗 Django URL Shortener

A simple **URL Shortener API** built using **Django REST Framework**.  
Users can shorten long URLs, optionally set custom aliases, and define expiration dates for the shortened links.

---

## 🚀 Features

- Shorten long URLs to a 6–8 character code  
- Custom alias support (e.g., `/learnpython`)  
- Optional expiration date for URLs  
- Redirect short URLs to their original long URLs  
- Graceful handling of code collisions  

---

## 🏗️ Project Structure

